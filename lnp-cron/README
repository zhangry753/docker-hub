php cron环境，基于lnp，读取/cron/crontab


部署步骤：

```
运行docker
    docker run -e LOG_MAXDAY=30 -d --restart=unless-stopped -v /<path to crontab>/crontab:/cron/crontab -v /data0/logs/cron:/logs --name=cron lnp-cron
```